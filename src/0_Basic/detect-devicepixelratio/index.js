window.onload = () => {
  console.log(window.devicePixelRatio);
  console.log(document.documentElement.clientWidth); // layout viewport
  console.log(window.innerWidth); // visual viewport
  // ideal viewport
};
